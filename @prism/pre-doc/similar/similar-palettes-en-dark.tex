% !TEX TS-program = lualatex

\documentclass[theme = dark]{tutodoc}

\usepackage[svgnames]{xcolor}
\usepackage[3d]{luadraw}
\usepackage{fourier}

\geometry{
  top    = 1.5cm,
  bottom = 1.5cm,
  left   = 1.75cm,
  right  = 1.75cm,
%  showframe
}

\directlua{dofile('../../products/luadraw/palettes.lua')}


\begin{document}

\begin{luacode*}
function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 200
  local dl    = L / N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end
\end{luacode*}

\pagestyle{empty}

\section*{Similar palettes}

This document includes some visually similar color palettes. The \texttt{@prism} project retains all of them to respect individual preferences and because subtle differences exist between them, however sometimes minimal.


\begin{tdocimp}
    Clusters were generated semi-automatically by a program suggesting similar palettes, with subsequent manual curation to keep only relevant groupings.
    This approach may lead to occasional omissions. If you identify any, please contact the author of \texttt{@prism}.
\end{tdocimp}




\subsection*{Cluster \#1}

\begin{luacode*}
PALETTES = {"Batlow", "BatlowW"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#2}

\begin{luacode*}
PALETTES = {"Blues", "PuBu"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#3}

\begin{luacode*}
PALETTES = {"BuGn", "Greens", "YlGn"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#4}

\begin{luacode*}
PALETTES = {"Bwr", "Seismic"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#5}

\begin{luacode*}
PALETTES = {"Davos", "Lapaz"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#6}

\begin{luacode*}
PALETTES = {"Inferno", "Magma"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#7}

\begin{luacode*}
PALETTES = {"Jet", "Turbo"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#8}

\begin{luacode*}
PALETTES = {"OrRd", "YlOrRd"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#9}

\begin{luacode*}
PALETTES = {"Oranges", "YlOrBr"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#10}

\begin{luacode*}
PALETTES = {"RdBu", "RdYlBu"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#11}

\begin{luacode*}
PALETTES = {"Roma", "RomaO"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#12}

\begin{luacode*}
PALETTES = {"TwilightShifted", "Vik", "VikO"}

for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\end{document}