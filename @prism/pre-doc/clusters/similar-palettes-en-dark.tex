% !TEX TS-program = lualatex

\documentclass[theme = dark]{tutodoc}

\usepackage[svgnames]{xcolor}
\usepackage[3d]{luadraw}
\usepackage{fourier}

\geometry{
  top    = 1.5cm,
  bottom = 1.5cm,
  left   = 1.75cm,
  right  = 1.75cm,
%  showframe
}

\directlua{dofile('../../products/luadraw/palettes.lua')}


\begin{document}

\pagestyle{empty}

\section*{Similar palettes -- Semi-automated process used}



\subsection*{Cluster \#1}

\begin{luacode*}
PALETTES = {"Afmhot", "Hot"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#2}

\begin{luacode*}
PALETTES = {"Bam", "PiYG"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#3}

\begin{luacode*}
PALETTES = {"Batlow", "BatlowK"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#4}

\begin{luacode*}
PALETTES = {"Binary", "Grays"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#5}

\begin{luacode*}
PALETTES = {"Broc", "BrocO"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#6}

\begin{luacode*}
PALETTES = {"Bwr", "Seismic"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#7}

\begin{luacode*}
PALETTES = {"Cork", "CorkO"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#8}

\begin{luacode*}
PALETTES = {"Gnuplot", "Plasma"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#9}

\begin{luacode*}
PALETTES = {"Imola", "Viridis"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#10}

\begin{luacode*}
PALETTES = {"Navia", "NaviaW"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#11}

\begin{luacode*}
PALETTES = {"OrRd", "Oranges", "Reds"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\subsection*{Cluster \#12}

\begin{luacode*}
PALETTES = {"RdYlBu", "Spectral"}

function Dpalette(name)
  local g = graph:new{
    window = {-5, 15, -5, 5},
    bbox   = false,
    border = false
  }

  g:Linewidth(1)

  local A     = Z(-5, 4)
  local h, dh = Z(0, -1), Z(0, -1.1)
  local L, N  = 15, 100
  local dl    = L/N

  local pal = getPal(name)

  for k = 1, N do
    local color = palette(pal, (k - 1) / (N - 1))

    g:Drectangle(
      A, A + h, A + h + dl,
         "color = " .. color
      .. ", fill = " .. color
    )

    A = A+dl
  end

  g:Drectangle(A, A + h, A + h - L)

  g:Dlabel(
    "\\quad\\large\\texttt{\\bfseries " .. name .. "}",
    A + h / 2,
    {pos = "E"}
  )

  g:Sendtotex()

  tex.sprint("\\smallskip\\par")
end


for _, name in pairs(PALETTES) do
  Dpalette(name)
end
\end{luacode*}

\end{document}