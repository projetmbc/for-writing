// ---------- //


==
??????  (2.0.0-beta.12)  ??????
==

-----
:new:
-----

[[:api:]]
\Contribs workflow simplified significantly.

    1) Data extraction and code generation are now performed by simply instantiating the 'PaletteTransformer' class.

    1) Monolithic or modular version can be omitted (\tex can't build all the high-fidelity palettes).

    1) Standardization of showcases with simplified graphics: waves for small palettes and random triangles for large ones.

               CSS      OK
               LaTeX    OK
               Lua      ???


-------
:break:
-------

[[:api:]]
\lua \prod: the lua::''getPal'' \func is now lua::''palCreateFromPal'' which wants a lua::''palette'' \var (no more name allowed).
