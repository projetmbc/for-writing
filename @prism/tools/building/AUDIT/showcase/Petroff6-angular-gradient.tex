% !TEX TS-program = lualatex

% ------------------------------------------- %
% -- AUTOMATICALLY GENERATED - DO NOT EDIT -- %
% ------------------------------------------- %

\documentclass{standalone}

\usepackage[svgnames]{xcolor}
\usepackage[3d]{luadraw}

\directlua{
  dofile('../../../../products/lua/palettes-hf/Petroff6.lua')
}

\begin{document}

\begin{luadraw}{name = Petroff6-angular-gradient}
require 'luadraw_shadedforms'

local g = graph:new{
  size   = {10,10},
  margin = {0,0,0,0},
  bbox   = false
}

L = circle(0, 1)

local f = function(x,y)
  return cpx.arg(Z(x,y))
end

g:Dshadedpolyline(
  L,
  palPetroff6,
  {
    values = f,
    width  = 300
  }
)

g:Show()
\end{luadraw}

\end{document}
