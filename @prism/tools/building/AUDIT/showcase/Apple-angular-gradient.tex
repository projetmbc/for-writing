% !TEX TS-program = lualatex

% ------------------------------------------- %
% -- AUTOMATICALLY GENERATED - DO NOT EDIT -- %
% ------------------------------------------- %

\documentclass{standalone}

\usepackage[svgnames]{xcolor}
\usepackage[3d]{luadraw}

\directlua{
  dofile('../../../../products/lua/palettes-hf/Apple.lua')
}

\begin{document}

\begin{luadraw}{name = Apple-angular-gradient}
require 'luadraw_shadedforms'

local g = graph:new{
  size   = {10,10},
  margin = {0,0,0,0},
  bbox   = false
}

L = circle(0, 1)

local f = function(x,y)
  return cpx.arg(Z(x,y))
end

g:Dshadedpolyline(
  L,
  palApple,
  {
    values = f,
    width  = 300
  }
)

g:Show()
\end{luadraw}

\end{document}
