\documentclass{article}
\usepackage{xcolor}
\usepackage{expl3}
\usepackage{tikz}

\ExplSyntaxOn

% ============================================
% DÉFINITION DU SYSTÈME DE PALETTES
% ============================================

% Créer une nouvelle palette
\cs_new_protected:Nn \palette_new:n
  {
    \seq_new:c { g_palette_#1_seq }
  }

% Ajouter une couleur RGB à une palette
\cs_new_protected:Nn \palette_add_color:nn
  {
    \seq_gput_right:cn { g_palette_#1_seq } { #2 }
    % Définir automatiquement la couleur avec un nom standard
    \int_set:Nn \l_tmpa_int { \seq_count:c { g_palette_#1_seq } }
    \definecolor{palette@#1@\int_use:N\l_tmpa_int}{rgb}{#2}
  }

% Ajouter plusieurs couleurs d'un coup (liste CSV)
\cs_new_protected:Nn \palette_add_colors:nn
  {
    \clist_map_inline:nn { #2 }
      {
        \palette_add_color:nn { #1 } { ##1 }
      }
  }

% Créer une palette avec toutes ses couleurs en une seule commande
\cs_new_protected:Nn \palette_create:nn
  {
    \palette_new:n { #1 }
    \palette_add_colors:nn { #1 } { #2 }
  }

% Obtenir le nombre de couleurs
\cs_new:Nn \palette_count:n
  {
    \seq_count:c { g_palette_#1_seq }
  }

% Obtenir une couleur par index
\cs_new:Nn \palette_get_color:nn
  {
    \seq_item:cn { g_palette_#1_seq } { #2 }
  }

% Obtenir le NOM d'une couleur (expandable, pour TikZ)
\cs_new:Nn \palette_get_color_name:nn
  {
    palette@#1@#2
  }

% Définir une couleur xcolor depuis une palette
\cs_new_protected:Nn \palette_define_xcolor:nnn
  {
    \definecolor{#1}{rgb}{\seq_item:cn{g_palette_#2_seq}{#3}}
  }

% Utiliser directement une couleur
\cs_new_protected:Nn \palette_use_color:nn
  {
    \color{palette@#1@#2}
  }

% Boucle sur toutes les couleurs d'une palette
\cs_new_protected:Nn \palette_map:nn
  {
    \int_step_inline:nn { \palette_count:n { #1 } }
      {
        \group_begin:
        \color{palette@#1@##1}
        #2
        \group_end:
      }
  }

% ============================================
% INTERFACE UTILISATEUR
% ============================================

\NewDocumentCommand \paletteNew { m }
  { \palette_new:n { #1 } }

\NewDocumentCommand \paletteAddColor { m m }
  { \palette_add_color:nn { #1 } { #2 } }

\NewDocumentCommand \paletteAddColors { m m }
  { \palette_add_colors:nn { #1 } { #2 } }

\NewDocumentCommand \palCreate { m m }
  { \palette_create:nn { #1 } { #2 } }

\NewDocumentCommand \paletteCount { m }
  { \palette_count:n { #1 } }

\NewDocumentCommand \paletteGetColor { m m }
  { \palette_get_color:nn { #1 } { #2 } }

\NewDocumentCommand \palDefineColor { m m m }
  { \palette_define_xcolor:nnn { #1 } { #2 } { #3 } }

\NewDocumentCommand \palColor { m m }
  { \palette_use_color:nn { #1 } { #2 } }

\NewDocumentCommand \paletteMap { m m }
  { \palette_map:nn { #1 } { #2 } }

\NewExpandableDocumentCommand \palUse { m m }
  { \palette_get_color_name:nn { #1 } { #2 } }

\ExplSyntaxOff

% ============================================
% EXEMPLE D'UTILISATION
% ============================================

\begin{document}

% ===== MÉTHODE PRINCIPALE : \palCreate =====
% Une seule commande pour créer une palette complète !

\newcommand\myname{Blackbody}

\palCreate{\myname}{
  {0.0, 0.0, 0.0}, 
  {0.4, 0.0, 0.2}, 
  {0.8, 0.2, 0.0}, 
  {1.0, 0.6, 0.0}, 
  {1.0, 1.0, 0.4}, 
  {1.0, 1.0, 1.0}
}

\palCreate{Ocean}{
  {0.0, 0.1, 0.3},
  {0.0, 0.3, 0.6},
  {0.2, 0.5, 0.8},
  {0.5, 0.7, 0.9},
  {0.8, 0.9, 1.0}
}

\palCreate{Sunset}{
  {1.0, 0.4, 0.0},
  {1.0, 0.6, 0.2},
  {1.0, 0.8, 0.4}
}

% Alternative : ajouter des couleurs après coup
% \paletteAddColor{Sunset}{1.0, 0.9, 0.6}

\section*{Démonstration des palettes}

\subsection*{Création simple}

\textbf{Syntaxe principale :}

\texttt{\textbackslash paletteCreate\{NomPalette\}\{\{r,g,b\}, \{r,g,b\}, ...\}}

\bigskip

Exemple :
\begin{verbatim}
\palCreate{Ocean}{
  {0.0, 0.1, 0.3},
  {0.0, 0.3, 0.6},
  {0.2, 0.5, 0.8}
}
\end{verbatim}

\subsection*{Palette Blackbody}
Nombre de couleurs : \paletteCount{Blackbody}

\bigskip

% Afficher toutes les couleurs
\paletteMap{Blackbody}{\rule{2cm}{1cm}\quad}

\bigskip

% Utilisation dans du texte
{\palColor{Blackbody}{1} Noir} \quad
{\palColor{Blackbody}{3} Rouge-orangé} \quad
{\palColor{Blackbody}{5} Jaune pâle} \quad
{\palColor{Blackbody}{6} Blanc}

\subsection*{Palette Ocean}
Nombre de couleurs : \paletteCount{Ocean}

\bigskip

\paletteMap{Ocean}{\rule{2cm}{1cm}\quad}

\subsection*{Palette Sunset}
Nombre de couleurs : \paletteCount{Sunset}

\bigskip

\paletteMap{Sunset}{\rule{2cm}{1cm}\quad}

\subsection*{Définir des couleurs nommées}

% Créer des couleurs xcolor depuis la palette
\palDefineColor{myred}{Blackbody}{3}
\palDefineColor{myblue}{Ocean}{3}

\textcolor{myred}{Texte en rouge} et \textcolor{myblue}{texte en bleu}.

\subsection*{Accès direct aux valeurs RGB}

La couleur 2 de Blackbody a les valeurs RGB : \paletteGetColor{Blackbody}{2}

\subsection*{Utilisation avec TikZ}

% Méthode 1 : Définir des couleurs nommées personnalisées
\palDefineColor{bb1}{Blackbody}{1}
\palDefineColor{bb2}{Blackbody}{2}
\palDefineColor{bb3}{Blackbody}{3}
\palDefineColor{bb4}{Blackbody}{4}
\palDefineColor{bb5}{Blackbody}{5}
\palDefineColor{bb6}{Blackbody}{6}

\bigskip

\begin{tikzpicture}
  % Rectangles Blackbody avec noms personnalisés
  \foreach \i in {1,...,6} {
    \fill[bb\i] (\i-1,0) rectangle (\i,1);
  }
  \node at (3,-0.5) {Blackbody (noms personnalisés)};
  
  % Rectangles Ocean avec \palUse
  \foreach \i in {1,...,5} {
    \fill[\palUse{Ocean}{\i}] (\i-1,2) rectangle (\i,3);
  }
  \node at (2.5,1.5) {Ocean (\textbackslash paletteColorName)};
  
  % Rectangles Sunset
  \foreach \i in {1,...,3} {
    \fill[\palUse{Sunset}{\i}] (\i-1+1.5,4) rectangle (\i+1.5,5);
  }
  \node at (2.5,3.5) {Sunset};
  
  % Cercles Blackbody
  \foreach \i in {1,...,6} {
    \fill[\palUse{Blackbody}{\i}] (7+\i*0.7, 0.5) circle (0.3);
  }
  
  % Cercles Ocean
  \foreach \i in {1,...,5} {
    \fill[\palUse{Ocean}{\i}] (7+\i*0.7, 2.5) circle (0.3);
  }
  
  % Cercles Sunset
  \foreach \i in {1,...,3} {
    \fill[\palUse{Sunset}{\i}] (8.5+\i*0.7, 4.5) circle (0.3);
  }
\end{tikzpicture}

\bigskip

\bigskip

\textbf{Création de palettes :}

\texttt{\textbackslash paletteCreate\{Nom\}\{\{r,g,b\}, \{r,g,b\}, ...\}} - Méthode recommandée

\bigskip

\textbf{Utilisation des couleurs :}
\begin{itemize}
\item Dans du texte : \texttt{\{\textbackslash paletteColor\{Nom\}\{index\} Texte\}}
\item Dans TikZ : \texttt{\textbackslash fill[\textbackslash paletteColorName\{Nom\}\{index\}] ...}
\item Nommée : \texttt{\textbackslash paletteDefineColor\{mycolor\}\{Nom\}\{index\}}
\end{itemize}

\textbf{Commandes avancées :}
\begin{itemize}
\item \texttt{\textbackslash paletteAddColor\{Nom\}\{r,g,b\}} : Ajouter une couleur
\item \texttt{\textbackslash paletteCount\{Nom\}} : Nombre de couleurs
\item \texttt{\textbackslash paletteMap\{Nom\}\{code\}} : Boucle sur toutes les couleurs
\end{itemize}

\end{document}